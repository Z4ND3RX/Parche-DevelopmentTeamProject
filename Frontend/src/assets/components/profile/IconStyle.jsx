import { Box, styled } from "@mui/material";

const IconoStyle = styled(Box)(({ theme }) => ({
  marginRight: "1rem",
  marginTop: "10px",
  fontSize: "1.2rem",
  verticalAlign: "middle",
}));

export default IconoStyle;
